#include "main.h"
/**
 * mul - multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply with the first number
 * Return: return mul results
 */
int mul(int a, int b)
{
	return (a * b);
}
